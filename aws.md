AWS

ses,sns,ec2---> (elastic cloud computer - a small computer) (for complex function - sns(systematic notification service), ses (systematic email service) )

RDS---> relational database service, just like accessing data (like postman);

s3 ---> cloud storage system ( image, video ) simple sotrage service.

load balancer ---> multiple service (handle queries, equal distribution)

IAM ----> identity and access management.

AET ---> amazon elastic transcode --> to stream media into multiple formats.

AWS websocket

amazon API getways.

---

scenario examples

1. serviceless microservices.

2. real-time chat application (aws websockets or socket.io)

---

DNS---> Domain Name System

DNS is decentralized naming system for internet connected resources.
DNS mantians a list of domain names along with the resources , such as ip address, that are associated with them.

the most prominent function of DNS is the translation of human-friendly domain names ( such flm.com) to numeric ip address ( such as 151.106.1.123);
this process of mapping a domain to name to the appropriate ip address is known as "DNS lookup"

www.example.com ==> 93.184.216.119

element of name

dot: at the end is the root\[first Zone]\
com: top level domain \[TLD]\ [second zone]\
example : domain \[third zone]\
www: subdomain\[forth zone]\

ipv4--> 192.168.1.1
ipv6---> 2001:0db8:8sdfk:0000:813:34
